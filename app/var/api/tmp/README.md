# Temporary Workspace

Use OS-level temp directories for experiments. If a script requires staging files, create a subfolder under `%TEMP%` instead. This directory stays empty aside from this README and `.gitkeep`.
